Read the `AGENTS.md` to get an understanding of the project. The task is to update the frontmatter of all markdown files.

The `kind` field of the parameter should always have a single item and not a list.

Here is an example of the `./API Key.md` file.

```markdown
---
kind: explain
section: term
---

# API Key

Geheimer Schlüssel für Zugriff auf Schnittstelle.

```

Afterwards it should look like this:

```markdown
---
kind: explain
section: term
---

# API Key

Geheimer Schlüssel für Zugriff auf Schnittstelle.

```
