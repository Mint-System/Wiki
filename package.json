{
  "scripts": {
    "vercel-build": "node build.js && vuepress build . && node post-build.js"
  },
  "devDependencies": {
    "vuepress": "^1.7.1"
  },
  "dependencies": {
    "fs-extra": "^9.0.1"
  }
}
