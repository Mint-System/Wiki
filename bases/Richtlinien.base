views:
  - type: table
    name: Table
    filters:
      and:
        - file.name.startsWith("Richtlinie")
        - file.ext == "md"
    order:
      - file.name
      - kind
    sort:
      - property: file.name
        direction: DESC
    columnSize:
      file.name: 610
