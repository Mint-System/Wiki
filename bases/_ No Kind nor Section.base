views:
  - type: table
    name: Table
    filters:
      and:
        - file.ext == "md"
        - or:
            - kind.isEmpty()
            - section.isEmpty()
        - '!file.name.endsWith("excalidraw")'
        - file.name.contains("odoo")
    order:
      - file.name
      - kind
      - section
      - file.tags
    sort:
      - property: kind
        direction: DESC
    columnSize:
      file.name: 610
