views:
  - type: table
    name: Table
    filters:
      or:
        - "!website.isEmpty()"
        - "!repository.isEmpty()"
    order:
      - file.name
      - website
      - repository
    sort:
      - property: repository
        direction: ASC
    columnSize:
      file.name: 610
