views:
  - type: table
    name: Table
    filters:
      or:
        - file.name.startsWith("Prozess")
        - file.name.startsWith("Process")
    order:
      - file.name
      - kind
      - tags
    sort:
      - property: file.name
        direction: DESC
    columnSize:
      file.name: 610
